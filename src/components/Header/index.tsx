import { HeaderContainer } from "./style";

import { Title } from "@tranqi/ui-kit";

const Header = () => {
  return (
    <HeaderContainer>
      <Title size={"small"}>
        {"Newsletters APP"}
      </Title>
    </HeaderContainer>
  );
};

export default Header;
